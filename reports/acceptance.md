# Приёмка прототипа — критерии M-1…M-7 (SRS §15)

Сводка статуса Definition of Done на конец Этапа 6. Пруфы — командами и ссылками на артефакты.

| # | Критерий | Порог | Статус | Пруф |
|---|---|---|---|---|
| **M-1** | Воспроизводимая сборка | образ ≤ 1 ГБ; ≤ 1 дня до карты | ✅ pass (CI) / ⚠️ локально | Сборка образа верифицируется в CI `docker-build-smoke` ([ci.yml](../.github/workflows/ci.yml), ubuntu, чистая сеть). Dockerfile ставит **CPU-torch** (не CUDA) → образ в рамках FR-19. Стратегия артефактов (монтирование томов) — [README §Docker](../README.md#docker). Локальная сборка на этой машине упирается в сеть Docker-VM (TLS timeout/EOF к PyPI/CDN — MTU WSL2), не в проект; воркэраунд — в README |
| **M-2** | Качество модели | U-Net > baseline по IoU∧F1∧PR-AUC, CI не пересекаются | ❌ **fail** (задокументированный, валидный) | [comparison_v1.md](comparison_v1.md): направленно лучше по всем метрикам, но CI пересекаются (32 test-тайла). Лимит — датасет, не код |
| **M-3** | Скорость инференса | p95 ≤ 5 с CPU (20 запросов) | ✅ pass | [latency.md](latency.md): p95 = **1.74 с**. `make bench` |
| **M-4** | UI-сценарии | 0 ошибок, без фризов > 2 с | ✅ pass | Прогон по HTTP-поверхности (uvicorn): predict ×3 сценария (агрегаты растут 0.055→0.106→0.184, 1.6–1.9 с), explain (топ-5 + 5 слоёв атрибуции), export (ZIP TIF+GeoJSON+PDF). При прогоне найден и исправлен баг overlay атрибуции (см. ниже); чек-лист — далее |
| **M-5** | Объяснимость | карта важности + метод и **ограничения** в README + пример | ✅ pass | [README §Объяснимость](../README.md#объяснимость-и-её-ограничения-m-5): метод, 5 ограничений, пример; UI-режим «Объяснение» |
| **M-6** | Документация | внешний человек поднимает систему ≤ 1 день | ✅ pass | [README.md](../README.md): предусловия → установка → демо → данные → обучение → метрики → Docker |
| **M-7** | Тесты + линт + покрытие | оба exit 0; coverage core ≥ 60 % | ✅ pass | `ruff` чисто; pytest **55 passed** (CI-выборка); coverage **~81 %** (≥ 60 %, NFR-11) |

**Итог:** 6/7 pass. Единственный fail — **M-2**, зафиксирован как валидный научный результат
(лимит датасета, не реализации); путь к pass — расширение датасета (отдельный R&D вне Этапа 6).

---

## M-4 — ручной чек-лист UI

Прогон по HTTP-поверхности сервиса (uvicorn) на пилотной территории (Тулун, bbox
`100.64,54.54,100.89,54.71`); поведение DOM атрибуции дополнительно смоделировано HTML-парсером:

- [x] `p1y`/`p10y`/`p100y` — overlay меняется: mean_p 0.055 → 0.106 → 0.184; p>0.5 0 → 0.077 → 0.124.
- [x] Агрегаты возвращаются во фрагменте и согласованы с растром (FR-16 OOB swap).
- [x] PNG overlay перепроецирован в EPSG:4326 и отдаётся (`/runs/<id>/prediction.png`, 200 image/png).
- [x] Экспорт → ZIP (1.79 МБ): `prediction.tif` + `aggregates.geojson` + `report.pdf`; `HX-Trigger: download-ready`.
- [x] Режим «Объяснение» → панель топ-5 (Землепокров 47% … DEM 5%) + **слой атрибуции** (5 PNG, magma).
- [x] Тайминги без фриза > 2 с: predict 1.6–1.9 с, explain ~6 с (< NFR-3 15 с), export 0.7 с.
- [x] Ошибки чисто: out-of-coverage bbox → сообщение (не 500); unknown run export → «запуск не найден».

> **Найдено и исправлено при прогоне M-4 (2026-06-05):** в `explanation_panel.html` атрибут
> `data-attribution-layers` рендерился в двойных кавычках вокруг `| tojson` — внутренние `"`
> JSON-а обрезали значение до `[{` в браузере → `JSON.parse` падал (тихо, через try/catch в
> [app.js](../static/app.js)), и **слой важности не накладывался на карту**. Фикс: двойные кавычки →
> одинарные (`tojson` экранирует `'`). После фикса DOM-значение — валидный JSON из 5 слоёв.
> Ранее (Этап 5) проверялся, видимо, только серверный ответ, не DOM.

---

## NFR-сводка

| NFR | Требование | Статус |
|---|---|---|
| NFR-1 | `/api/predict` p95 ≤ 5 с CPU | ✅ 1.74 с ([latency.md](latency.md)) |
| NFR-3 | `/api/explain` p95 ≤ 15 с CPU | ✅ ~5.1 с (Этап 5) |
| NFR-7 | Воспроизводимость метрик (±2 % IoU, OQ-8) | ⚠️ не измерялась (known-gap, см. [README](../README.md#ограничения-и-known-gaps)) |
| NFR-11 | Покрытие core ≥ 60 % | ✅ ~81 % |
| NFR-12 | `ruff check` / `format --check` exit 0 | ✅ |
