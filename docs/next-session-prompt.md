# Промт для новой сессии (floodrisk)

> Скопируй текст ниже как первое сообщение новой сессии.

---

# floodrisk — рефакторинг + допиливание до идеала

Проект `floodrisk` (`c:\Users\hekit\my_projects\floodrisk`). Академический прототип под защиту
диссертации: веб-сервис, который по bbox территории и метео-сценарию (p1y/p10y/p100y) отдаёт
растровую карту ПРОСТРАНСТВЕННОЙ подверженности затоплению (вероятность ∈[0,1] на пиксель),
построенную U-Net по рельефу. ВАЖНО: это НЕ прогноз во времени; «будущее» = сценарий осадков.

## === СНАЧАЛА ПРОЧИТАЙ ===
1. Авто-память (грузится через `MEMORY.md`): особенно `project_floodrisk`, `preprocess-mosaic-bugfix`,
   `multiregion-kansk-readiness`, `stage2-ml-results`, `ux-cluster3-map-validation`,
   `global-online-mode`, `workflow-preferences`, `ux-cluster1-readable-map`, `ux-cluster2-scientific-narrative`.
2. `docs/srs.md` — финальное ТЗ, ВЫСШИЙ приоритет при расхождениях (стек/анти-голы заморожены).
   `docs/onboarding.md` — карта проекта (что/зачем/где править) + FAQ. `README.md` — запуск.
   `reports/acceptance.md` — критерии M-1…M-7 + раздел про багфикс/мультирегион.
3. Подними приложение и ПОЩУПАЙ текущий UX руками, прежде чем рефакторить.

## === ТЕКУЩЕЕ СОСТОЯНИЕ (всё зелёное: ruff чисто, pytest 88 passed) ===
- Этапы 0–6 ЗАКРЫТЫ. Сверх плана добавлено (см. onboarding «Расширения после Этапа 6»):
  - **Глобальный онлайн-режим** (инференс по любому bbox; `online_features.py`; source=auto/mosaic/online).
  - **UX-кластеры 1–3**: рисуемый bbox, инспектор точки, метаданные, прозрачность; swipe U-Net↔baseline;
    **валидация «реальность S1 ↔ предсказание»** (`validation.py`, `GET /api/runs/{id}/groundtruth`,
    живые IoU/F1); геокодер (Nominatim), permalink (bbox/сценарий/модель в URL), подложка-спутник (Esri).
  - **🛠 КРИТ-багфикс признаков (главное прошлой сессии):** `run_preprocess` мозаичил ОДИН тайл
    (`_first`) вместо всех перекрывающих сетку → рельеф был пуст в ~94% площади; модель v1 училась
    на вырожденных данных. Фикс — `preprocess.mosaic_source_to_file` (+ bbox-тег имён против коллизий
    регионов в общем `data/raw/`). Тулун перестроен (DEM 98.6%), U-Net+baseline ПЕРЕОБУЧЕНЫ, M-2
    переоценён на ВАЛИДНЫХ данных → всё ещё FAIL, но честно (лимит = разнообразие данных, не метод).
  - **Мультирегион (демо):** 2-й регион **Канск** (р. Кан, EPSG:32646). Инференс выбирает региональную
    мозаику по bbox (`service._mosaic_stacks`/`mosaic_coverages`); на карте 2 контура покрытия; explain
    и валидация мультирегиональны. Канск помечен `experimental` (S1-лейблы шумные 6.8%, без эталона
    EMS → в M-2 не входит, в UI кавеат). `ml/combine.py` + мультирегион-`tile_paths` — SCAFFOLDING под
    обучение на 2+ событиях, НЕ задействовано (v2-конфиги удалены).

## === СТЕК ЖЁСТКО ЗАФИКСИРОВАН (SRS §3), НЕ менять без согласования ===
Python 3.11; FastAPI + SQLModel + SQLite; фронт — ОДНА HTML-страница на HTMX + Alpine.js + Tailwind +
Leaflet через CDN, БЕЗ SPA/бандлеров/npm. ML: PyTorch + smp + captum; uv для зависимостей. Анти-голы
(НЕ делать): React/Vue/бандлеры, Postgres, мобилка/PWA, аутентификация, real-time, прогноз во времени,
гидродинамика/глубина воды, оценка ущерба.

## === ОКРУЖЕНИЕ И КОМАНДЫ (Windows, PowerShell, make НЕ установлен) ===
- Python — всегда из `.venv` (3.11), extras `[data]` и `[ml]` УЖЕ стоят в рабочем `.venv`.
- Перед командами: `Set-Location "C:\Users\hekit\my_projects\floodrisk"`; python как `& ".\.venv\Scripts\python.exe"`.
- Сервер: `& ".\.venv\Scripts\python.exe" -m uvicorn floodrisk.app:app --port 8000` → http://127.0.0.1:8000.
  Нет сценариев/моделей → `& ".\.venv\Scripts\python.exe" -m floodrisk.db.seed` (из КОРНЯ).
- CI-ГЕЙТ перед каждым «готово»:
  `& ".\.venv\Scripts\python.exe" -m ruff format --check . ; ruff check . ; pytest -m "not requires_network and not ml_smoke" -q`
- ГОЧИ (из памяти): data/ML-шаги НЕ параллелить (гонки за файлы); верить только rc+содержимому файлов;
  после переобучения — РЕСТАРТ uvicorn (резидентная модель кэшируется); перед переобучением удалить
  `data/processed/<ds>/norm_stats.json` (пересчёт на актуальных данных); JSON-объекты в HTML-атрибут →
  атрибут в ОДИНАРНЫХ кавычках (`tojson` не экранирует `"`).

## === ТВОЯ ЗАДАЧА (по порядку) ===

### Этап A — ПОЛНЫЙ РЕФАКТОРИНГ кода, структуры, чистка (СНАЧАЛА)
Цель: привести проект к идеалу инженерно, НЕ меняя поведения и не ломая зелёный CI (88 passed) и
демо. Действуй через план (EnterPlanMode) для крупных перестроек. Кандидаты (оцени и дополни):
- **Чистка артефактов:** `runs/_*.log`, `runs/_srv*.out/err`, `runs/_kansk_gate*.png` (мои скретчи);
  устаревшие чекпойнты в `models/unet/v1/` (`last.ckpt`, старые `*epoch*` от обучения на битых данных —
  оставить только актуальный + `model.ts.pt` + `best.txt`); проверить `.gitignore` покрывает.
- **Мёртвый/scaffolding-код:** решить судьбу `ml/combine.py` + мультирегион-ветки `tile_paths`
  (оставить с тестами как фундамент v2 ИЛИ убрать); проверить отсутствие прочего dead code.
- **Структура/нейминг/типизация:** единообразие модулей `inference/`/`data/`/`ml/`, докстринги,
  type hints, разнести при необходимости разросшиеся файлы (напр. `service.py`); убрать дублирование
  (PNG-репроекция повторяется в `service._write_png_wgs84` и `validation._write_mask_png_wgs84` —
  вынести общий хелпер).
- **Тесты:** не уронить покрытие (core ≥60%, факт ~81%); тесты под рефакторинг подправить.
- Каждое изменение — под CI-гейт; существующее поведение (предикт/explain/export/валидация/мультирегион/
  онлайн) проверять в реальном браузере.

### Этап B — допиливание фич до идеала (ПОСЛЕ рефакторинга, по согласованию)
Веди инициативу, КАЖДУЮ идею оценивай по эффективности и НАДОБНОСТИ (демо для одного пользователя под
защиту, без gold-plating). Кандидаты из прошлого бэклога + новые:
- **«Горячие точки риска» (B3)** — авто-поиск топ-N кластеров высокой вероятности (связные компоненты
  по prob-карте) + список/зум. Демонстрируемо, «actionable». Средняя трудоёмкость.
- **Чистые лейблы 2-го события + обучение на 2+ событиях** (атака на M-2): тюнинг S1-детектора
  (сузить bbox до поймы, пороги Otsu/change-detection, найти Copernicus EMS-эталон), затем `combine.py`
  → train v2 → eval. Тяжёлый R&D; M-2 может сдвинуться. Согласовать глубину.
- **OQ-8** (дисперсия метрик между сидами) — обучить U-Net 2–3 раза, замерить разброс IoU/F1.
- Что ещё посчитаешь нужным для «идеальной защиты» — предложи в приоритизированном бэклоге
  (проблема → предложение → польза → трудоёмкость → риск → вердикт).

## === РАБОЧИЕ ПРЕДПОЧТЕНИЯ ===
Действуй решительно, мало вопросов. Перед крупным кодом — план (EnterPlanMode), согласуй направление,
НЕ начинай крупное без «ок». Мелочи — сразу. Data/ML-шаги последовательно. Верь exit-кодам и файлам.
Перед «готово» — CI-гейт. Обновляй авто-память по итогам. Git коммитит пользователь (предлагай команды).
В начале проверь `git status` (прошлая сессия НЕ коммитила — изменения ждут коммита пользователем).
