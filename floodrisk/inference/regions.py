"""Реестр офлайн-регионов с мозаикой признаков (`data/processed/<region>/...`).

Единый источник человекочитаемых подписей и статуса валидации. Используется
``service`` (выбор/подписи покрытий, порядок зон в UI) и ``validation`` (флаг
``experimental`` для невалидированных регионов).

- ``REGION_LABELS`` — код региона → подпись для UI.
- ``VALIDATED_REGIONS`` — регионы с проверенными лейблами/эталоном. Тулун (``v1``)
  обучен и проверен; Канск (``kansk``) — демо: признаки корректны, но S1-маска не
  валидирована эталоном (пере-детекция) → помечается experimental, в M-2 не входит.
"""

from __future__ import annotations

REGION_LABELS = {"v1": "Тулун", "kansk": "Канск"}
VALIDATED_REGIONS = {"v1"}
