"""ML training pipeline: U-Net + baseline + MLflow. См. SRS §7, §11.2.

Этот подпакет требует extras [ml] (pytorch-lightning, smp, albumentations, mlflow).
В app/inference Docker-образе он не используется.
"""
